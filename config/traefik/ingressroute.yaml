apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: homelab-ingressroute
  namespace: kube-system
spec:
  entryPoints:
    - web
  routes:
  - match: Host(`homelab.local`)
    kind: Rule
    services:
    - name: traefik
      port: 80
